@model IEnumerable<BOOKMVCCORE.Models.Publicsh>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PublisherName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Country)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PublisherName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Country)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PublicsId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PublicsId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PublicsId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
